import React from 'react'
import Modal from '../../components/Modal'

const AddList = ({onUnderlayClick}) => {
    return (
        <Modal onUnderlayClick={onUnderlayClick}>addlist</Modal>
    )
}

export default AddList
